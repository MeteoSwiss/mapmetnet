mapmetnet.utils
===============

.. py:module:: mapmetnet.utils

.. autoapi-nested-parse::

   Copyright (c) 2026 MeteoSwiss, contributors listed in AUTHORS.

   Distributed under the terms of the 3-Clause BSD License.

   SPDX-License-Identifier: BSD-3-Clause

   Module contains: general utility functions



Attributes
----------

.. autoapisummary::

   mapmetnet.utils.logger


Functions
---------

.. autoapisummary::

   mapmetnet.utils.pad_angular_range
   mapmetnet.utils.squarify_extent
   mapmetnet.utils.is_overlapping
   mapmetnet.utils.get_circle_geom
   mapmetnet.utils.get_overlap_geom
   mapmetnet.utils.set_mplstyle


Module Contents
---------------

.. py:data:: logger

.. py:function:: pad_angular_range(ang_range: Union[list, tuple, numpy.ndarray], pad_fraction: float)

   Pad a given angular range by a fraction of the range (on both sides).

   :Parameters: * **ang_range** (*list, tuple, ndarray*) -- len(2) list/tuple/ndarray with lower and higher angular
                  bounds.
                * **pad_fraction** (*float*) -- padding fraction. Setting pad_fraction to 0 returns the same range.

   Return: list or tuple or ndarray, padded by the requested amount.



.. py:function:: squarify_extent(lon_lims: numpy.ndarray, lat_lims: numpy.ndarray)

   Adjust longitude and latitude limits to get a squar-ish map extent.

   :Parameters: * **lon_lims** (*tuple*) -- min/max longitudes
                * **lat_lims** (*tuple*) -- min/max latitudes

   Return: tuple of (lon_lims, lat_lims) with the narrowest direction suitably expanded.



.. py:function:: is_overlapping(geom: shapely.geometry, extent: tuple) -> bool

   Utility function to check if a given geometry overlapps with a plot extent.

   :Parameters: * **geom** (*shapely geometry*) -- a geometry to check
                * **extent** (*tuple*) -- the map extent, from ax.get_extent(crs=ccrs.PlateCarree())

   Returns: bool



.. py:function:: get_circle_geom(lon: float, lat: float, radius: float)

   Given a lat-lon positon and radius, build a circle geometry.

   :Parameters: * **lon** (*float*) -- center longitude
                * **lat** (*float*) -- center latitude
                * **radius** (*float*) -- radius in km

   Returns: shapely.geometry



.. py:function:: get_overlap_geom(geoms: list) -> shapely.geometry

   Given a list of geometries, assemble the region where two (or more) overlap. It does not
   need to be a contiguous region.

   :Parameters: **geoms** (*list*) -- list of shapely geometries to process.

   :returns: the shapely geometry of the overlaps
   :rtype: sgeom


.. py:function:: set_mplstyle(func: Callable) -> Callable

   Intended to be used as a decorator around plotting functions, to set the plotting style.

   :returns: the decorator.
   :rtype: Callable

   Adapted from the ampycloud source code: https://github.com/MeteoSwiss/ampycloud

   This is a simpler version of the Decorator that only loads the basic matplotlib style
   specified in mpl_styles/base.mplstyle.



